upstream webapp {
    server flask-app:5005;
}

server {
    listen 80;
    server_name j8iraids.com 193.53.40.252;

    # ✅ Allow Certbot verification requests (Do NOT redirect these to HTTPS)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
    }

    # 🚀 Temporarily disable HTTPS redirection until SSL is issued
    location / {
        proxy_pass http://webapp;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Host $host:$server_port;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# ❌ Remove or comment out this block until Certbot issues the certificate
# server {
#     listen 443 ssl;
#     server_name j8iraids.com 193.53.40.252;
#
#     ssl_certificate /etc/letsencrypt/live/j8iraids.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/j8iraids.com/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://webapp;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header Host $host:$server_port;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }
